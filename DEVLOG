### Day 1
- Read through the full assignment description carefully
- Broke the project into smaller parts (stack, tokenizer, validation, conversion, evaluation)
- Created GitHub repository and initial files (main.cpp, ArrayStack.h, DEVLOG.md)
- Defined Token struct and function prototypes
- Did not implement logic yet, focused on planning structure

### Forgot to move my work from CLion over to the Github sorry about the commit but here was my second day of work
- Implemented ArrayStack class using std::vector
- Completed push, pop, top, empty, and size methods
- Added error handling for pop and top on empty stack
- Tested stack manually with simple values to confirm behavior
- Verified LIFO behavior works correctly

### Forgot to add my stuff to DEVLOG last night but I'm just gonna add the rest of my work in increments today, I finished but I forgot to keep an update of DEVLOG and need to move my readme from my laptop over.
- Implemented tokenizer to convert input string into tokens
- Handled multi-digit numbers correctly by building number strings
- Ignored whitespace and separated operators and parentheses
- Tested tokenizer with multiple expressions (infix and postfix)
- Added temporary debug output to verify token sequence
- Fixed issue where numbers were being split incorrectly

### Postfix validation & evaluation
- Finished and moved out array stack
- Implemented postfix validation using stack depth logic
- Ensured operators require two operands
- Tested valid and invalid postfix expressions
- Verified final depth must equal 1
- Implemented postfix evaluation using ArrayStack
- Parsed numbers using stod
- Applied correct operator logic for +, -, *, /
- Tested evaluation with multiple postfix expressions
